
#spring.profiles.active=@profileActive@
spring.profiles.active=${profile_active:dev}

# Number of ms to wait before throwing an exception if no connection is available.
spring.datasource.tomcat.max-wait=10000
# Maximum number of active connections that can be allocated from this pool at the same time.
spring.datasource.tomcat.max-active=500

# Validate the connection before borrowing it from the pool.
spring.datasource.tomcat.test-on-borrow=true
spring.datasource.max-idle=8
spring.datasource.min-evictable-idle-time-millis=100
spring.datasource.min-idle=8 

#hibernate common
hibernate.id.new_generator_mappings=true
entitymanager.packagesToScan=sskf
jwt.secret=javainuseEAlabw8234jAkjh4

spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB